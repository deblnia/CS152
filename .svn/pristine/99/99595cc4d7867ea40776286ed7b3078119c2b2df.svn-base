#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "warmup2.h"  // note this new include file!!!

// TODO: Write a test function for each exercise

/* Principle 2: Design a good set of test cases
 * they check the base case (0), right above the base case (1), well
 * above the base case (5, 8), and error conditions (-1, -5).
 * -1, 0, 1 are the boundary test cases - base, base+1, base-1.
 */
int main()
{

	 
	// Testing letter prints
	fprintf(stdout, "Testing print_asterisk_letter S/T/U/V\n");  
	print_asterisk_letter('S');
	print_asterisk_letter('T');
	print_asterisk_letter('U');
	print_asterisk_letter('V');
	fprintf(stdout, "Testing draw_hourglass_rec height 10\n");  
	draw_hourglass_rec(10);	
	fprintf(stdout, "Testing draw_hourglass_rec height 11\n");  
	draw_hourglass_rec(11);
	fprintf(stdout, "Testing draw_hourglass_rec height 1\n");  
	draw_hourglass_rec(1);
	fprintf(stdout, "Testing draw_hourglass_rec height 2\n");  
	draw_hourglass_rec(2);
	fprintf(stdout, "Testing draw_hourglass_rec height 3\n");  
	draw_hourglass_rec(3);
	fprintf(stdout, "Testing draw_hourglass_rec height 0\n");   
	draw_hourglass_rec(0);
	fprintf(stdout, "Testing draw_hourglass_iter height 10\n");   
	draw_hourglass_iter(10);
	fprintf(stdout, "Testing draw_hourglass_iter height 11\n");   
	draw_hourglass_iter(11); 
	fprintf(stdout, "Testing draw_hourglass_iter height 1\n");   
	draw_hourglass_iter(1); 
	fprintf(stdout, "Testing draw_hourglass_iter height 2\n");  
	draw_hourglass_iter(2); 
	fprintf(stdout, "Testing draw_hourglass_iter height 3\n");   
	draw_hourglass_iter(3);
	fprintf(stdout, "Testing draw_hourglass_iter height 0\n");   
        draw_hourglass_iter(0);
}

